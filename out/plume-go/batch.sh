#!/bin/bash
set -o pipefail
# DRAFT -- NOT SUBMITTED. Round 8 / batch plume-go: docs/audits/plume_goal_only.md.
# 36 jobs = 3 arms x 2 start sets x 6 runs (brain seeds 31-36), one B=6 room per job,
# 2 cluster_run.py calls of <= 24; every job pinned to one GPU of the pool 4,5,6,7 (gpus 1 + gpu_ids [one id]).
# The second cluster node is named by the CLUSTER_NODE_2 environment variable (docs/CLUSTER.md, git-ignored); committed
# text never carries the name. Generated by scripts/plume_goal_only.py plan on 2026-09-18T00:14:43Z.
# Read: `ls out/plume-go/*_r?.json | wc -l` = 36, `grep -c 'device cuda' out/plume-go/*_r?.txt`, then analyse (zero problems).
python scripts/cluster_run.py --target house --node "${CLUSTER_NODE_2:?set CLUSTER_NODE_2 to the name of the second cluster node}" --gpu-ids 4,5,6,7 --ship flyverse,scripts --name plume-go --minutes 90 "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm full --starts shipped --run 0 --out out/plume-go/full_shipped_r0 > out/plume-go/full_shipped_r0.txt 2>&1; st=\$?; tail -3 out/plume-go/full_shipped_r0.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm goal-only --starts shipped --run 0 --out out/plume-go/goal-only_shipped_r0 > out/plume-go/goal-only_shipped_r0.txt 2>&1; st=\$?; tail -3 out/plume-go/goal-only_shipped_r0.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm feedback-only --starts shipped --run 0 --out out/plume-go/feedback-only_shipped_r0 > out/plume-go/feedback-only_shipped_r0.txt 2>&1; st=\$?; tail -3 out/plume-go/feedback-only_shipped_r0.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm full --starts shipped --run 1 --out out/plume-go/full_shipped_r1 > out/plume-go/full_shipped_r1.txt 2>&1; st=\$?; tail -3 out/plume-go/full_shipped_r1.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm goal-only --starts shipped --run 1 --out out/plume-go/goal-only_shipped_r1 > out/plume-go/goal-only_shipped_r1.txt 2>&1; st=\$?; tail -3 out/plume-go/goal-only_shipped_r1.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm feedback-only --starts shipped --run 1 --out out/plume-go/feedback-only_shipped_r1 > out/plume-go/feedback-only_shipped_r1.txt 2>&1; st=\$?; tail -3 out/plume-go/feedback-only_shipped_r1.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm full --starts shipped --run 2 --out out/plume-go/full_shipped_r2 > out/plume-go/full_shipped_r2.txt 2>&1; st=\$?; tail -3 out/plume-go/full_shipped_r2.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm goal-only --starts shipped --run 2 --out out/plume-go/goal-only_shipped_r2 > out/plume-go/goal-only_shipped_r2.txt 2>&1; st=\$?; tail -3 out/plume-go/goal-only_shipped_r2.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm feedback-only --starts shipped --run 2 --out out/plume-go/feedback-only_shipped_r2 > out/plume-go/feedback-only_shipped_r2.txt 2>&1; st=\$?; tail -3 out/plume-go/feedback-only_shipped_r2.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm full --starts shipped --run 3 --out out/plume-go/full_shipped_r3 > out/plume-go/full_shipped_r3.txt 2>&1; st=\$?; tail -3 out/plume-go/full_shipped_r3.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm goal-only --starts shipped --run 3 --out out/plume-go/goal-only_shipped_r3 > out/plume-go/goal-only_shipped_r3.txt 2>&1; st=\$?; tail -3 out/plume-go/goal-only_shipped_r3.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm feedback-only --starts shipped --run 3 --out out/plume-go/feedback-only_shipped_r3 > out/plume-go/feedback-only_shipped_r3.txt 2>&1; st=\$?; tail -3 out/plume-go/feedback-only_shipped_r3.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm full --starts shipped --run 4 --out out/plume-go/full_shipped_r4 > out/plume-go/full_shipped_r4.txt 2>&1; st=\$?; tail -3 out/plume-go/full_shipped_r4.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm goal-only --starts shipped --run 4 --out out/plume-go/goal-only_shipped_r4 > out/plume-go/goal-only_shipped_r4.txt 2>&1; st=\$?; tail -3 out/plume-go/goal-only_shipped_r4.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm feedback-only --starts shipped --run 4 --out out/plume-go/feedback-only_shipped_r4 > out/plume-go/feedback-only_shipped_r4.txt 2>&1; st=\$?; tail -3 out/plume-go/feedback-only_shipped_r4.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm full --starts shipped --run 5 --out out/plume-go/full_shipped_r5 > out/plume-go/full_shipped_r5.txt 2>&1; st=\$?; tail -3 out/plume-go/full_shipped_r5.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm goal-only --starts shipped --run 5 --out out/plume-go/goal-only_shipped_r5 > out/plume-go/goal-only_shipped_r5.txt 2>&1; st=\$?; tail -3 out/plume-go/goal-only_shipped_r5.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm feedback-only --starts shipped --run 5 --out out/plume-go/feedback-only_shipped_r5 > out/plume-go/feedback-only_shipped_r5.txt 2>&1; st=\$?; tail -3 out/plume-go/feedback-only_shipped_r5.txt; exit \$st" --fetch out/plume-go/ 2>&1 | tee out/plume-go/client_stdout_0.txt || exit $?
python scripts/cluster_run.py --target house --node "${CLUSTER_NODE_2:?set CLUSTER_NODE_2 to the name of the second cluster node}" --gpu-ids 4,5,6,7 --ship flyverse,scripts --name plume-go --minutes 90 "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm full --starts drawn --run 0 --out out/plume-go/full_drawn_r0 > out/plume-go/full_drawn_r0.txt 2>&1; st=\$?; tail -3 out/plume-go/full_drawn_r0.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm goal-only --starts drawn --run 0 --out out/plume-go/goal-only_drawn_r0 > out/plume-go/goal-only_drawn_r0.txt 2>&1; st=\$?; tail -3 out/plume-go/goal-only_drawn_r0.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm feedback-only --starts drawn --run 0 --out out/plume-go/feedback-only_drawn_r0 > out/plume-go/feedback-only_drawn_r0.txt 2>&1; st=\$?; tail -3 out/plume-go/feedback-only_drawn_r0.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm full --starts drawn --run 1 --out out/plume-go/full_drawn_r1 > out/plume-go/full_drawn_r1.txt 2>&1; st=\$?; tail -3 out/plume-go/full_drawn_r1.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm goal-only --starts drawn --run 1 --out out/plume-go/goal-only_drawn_r1 > out/plume-go/goal-only_drawn_r1.txt 2>&1; st=\$?; tail -3 out/plume-go/goal-only_drawn_r1.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm feedback-only --starts drawn --run 1 --out out/plume-go/feedback-only_drawn_r1 > out/plume-go/feedback-only_drawn_r1.txt 2>&1; st=\$?; tail -3 out/plume-go/feedback-only_drawn_r1.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm full --starts drawn --run 2 --out out/plume-go/full_drawn_r2 > out/plume-go/full_drawn_r2.txt 2>&1; st=\$?; tail -3 out/plume-go/full_drawn_r2.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm goal-only --starts drawn --run 2 --out out/plume-go/goal-only_drawn_r2 > out/plume-go/goal-only_drawn_r2.txt 2>&1; st=\$?; tail -3 out/plume-go/goal-only_drawn_r2.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm feedback-only --starts drawn --run 2 --out out/plume-go/feedback-only_drawn_r2 > out/plume-go/feedback-only_drawn_r2.txt 2>&1; st=\$?; tail -3 out/plume-go/feedback-only_drawn_r2.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm full --starts drawn --run 3 --out out/plume-go/full_drawn_r3 > out/plume-go/full_drawn_r3.txt 2>&1; st=\$?; tail -3 out/plume-go/full_drawn_r3.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm goal-only --starts drawn --run 3 --out out/plume-go/goal-only_drawn_r3 > out/plume-go/goal-only_drawn_r3.txt 2>&1; st=\$?; tail -3 out/plume-go/goal-only_drawn_r3.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm feedback-only --starts drawn --run 3 --out out/plume-go/feedback-only_drawn_r3 > out/plume-go/feedback-only_drawn_r3.txt 2>&1; st=\$?; tail -3 out/plume-go/feedback-only_drawn_r3.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm full --starts drawn --run 4 --out out/plume-go/full_drawn_r4 > out/plume-go/full_drawn_r4.txt 2>&1; st=\$?; tail -3 out/plume-go/full_drawn_r4.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm goal-only --starts drawn --run 4 --out out/plume-go/goal-only_drawn_r4 > out/plume-go/goal-only_drawn_r4.txt 2>&1; st=\$?; tail -3 out/plume-go/goal-only_drawn_r4.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm feedback-only --starts drawn --run 4 --out out/plume-go/feedback-only_drawn_r4 > out/plume-go/feedback-only_drawn_r4.txt 2>&1; st=\$?; tail -3 out/plume-go/feedback-only_drawn_r4.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm full --starts drawn --run 5 --out out/plume-go/full_drawn_r5 > out/plume-go/full_drawn_r5.txt 2>&1; st=\$?; tail -3 out/plume-go/full_drawn_r5.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm goal-only --starts drawn --run 5 --out out/plume-go/goal-only_drawn_r5 > out/plume-go/goal-only_drawn_r5.txt 2>&1; st=\$?; tail -3 out/plume-go/goal-only_drawn_r5.txt; exit \$st" "mkdir -p out/plume-go && source .venv/bin/activate && python -c 'import torch; assert torch.cuda.is_available()' && PYTHONIOENCODING=utf-8 python scripts/plume_goal_only.py room --arm feedback-only --starts drawn --run 5 --out out/plume-go/feedback-only_drawn_r5 > out/plume-go/feedback-only_drawn_r5.txt 2>&1; st=\$?; tail -3 out/plume-go/feedback-only_drawn_r5.txt; exit \$st" --fetch out/plume-go/ 2>&1 | tee out/plume-go/client_stdout_1.txt || exit $?
