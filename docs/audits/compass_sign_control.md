# The V- arm: the sign-flipped afferent alone (round 8, item 2; batch `cx9`)

Question (TODO.md section B, follow-up 3 of the Session-13 skeptics): round 7 found V minus S raises GLNO L-R by
+2.2183 Hz (`result`, Holm p 0.0130; [compass_velocity_route.md](compass_velocity_route.md) 5.2), and its sign control
HGV- -- the flipped afferent on the held, relabelled ring -- did not reverse GLNO L-R (+0.6862 +/- 1.9132 Hz, positive
in four of six seeds). No sign-flipped counterpart of V itself was declared, so the sign specificity of primary 3 was
untested where it was measured. This batch declares it: the afferent at k 0.5 with sign -1, no hold, no relabel,
beside re-runs of S and V. Nothing is adopted; no gain is selected; no compass room is unlocked.

## 1. Arms and protocol

cx8r's prescribed-turn `cx_wedge` protocol, unchanged: full connectome, no world, compass adaptation 0, 10 Hz Poisson
background on the 46 EPG, 1 s settle, wedges 0-3 at +40 Hz for 2 s, 5 s free, a prescribed +90 deg/s turn over
0.5-3.5 s after the pulse end, fed to the afferent when one is attached; `--receptor-model shipped` (sign / abs); the
job lines are `cx_velocity_route.arm_command` / `job_line` verbatim (`out/cx9/batch.sh`).

| arm | preset | instrument | role |
|---|---|---|---|
| S | raw | -- | reference (cx8r's S, re-run) |
| V | instrumented | `sided_turn_afferent:k=0.5` | the afferent alone (cx8r's V, re-run) |
| V- | instrumented | `sided_turn_afferent:k=0.5:sign=-1` | the arm: the sign-flipped afferent alone |

Six seeds (0-5) per arm, 18 jobs, one `cluster_run.py` call, every job pinned to one GPU of the pool 4-7 of the second
node (`--gpu-ids 4,5,6,7 --node <cluster-node-2>`), `--ship flyverse,scripts` because the box checkout is behind
`origin/main` ([determinism_gate.md](determinism_gate.md) section 2).

## 2. Predeclaration (`out/cx9/predeclared.json`, stamped 2026-09-17T23:43:39Z; submitted as run `cx9-2daa4d` at ~23:44Z)

One Holm family, m = 3 (6 v 6 exact-U floor 0.0021645 x 3 = 0.0065, satisfiable):

| test | key | contrast | predicted sign | why |
|---|---|---|---|---|
| 1 | `GLNO_LR_hz` | V- minus S | negative | the mirror of round 7's V minus S = +2.2183 |
| 2 | `GLNO_LR_hz` | V minus V- | positive | the sign flip |
| 3 | `PS196b_LR_hz` | V minus V- | negative | V's PS196_b L-R was -18.16 Hz; the flipped afferent should reverse it |

Verdicts from `flyverse.interp.common.compare` (result / null / underpowered / undetermined); a `result` with the
unpredicted sign is reported as `result, opposite sign` and is no confirmation. Descriptive, no verdict: the per-side
turn-window rates (the pedestal) beside every L-R. Replication check: cx8r's S and V beside cx9's, per key, exact-equal
seeds and max |diff| -- an observation across two nodes and two submissions, never an inference. Outcome rule:
primaries 1 and 3 negative results and 2 a positive result = the GLNO side report follows the afferent's sign;
anything else = the sign specificity of round 7's primary 3 is not established.

## 3. Results (`out/cx9/analysis/`, generated by `scripts/cx_sign_control.py --analyse`; 18 runs, 0 problems)

Every run: `NVIDIA B200`, device `cuda` in 18/18 consoles, compiled cache `ef23cc27bea13be7f6a96f3c04fd3737` (the
shipped cache, 18/18), preset and instrument list as tabled, resolved LIF equal to the frozen record, every loaded
source file at the predeclared hash, wall 40.5-96.0 s. The scheduler listed 16 jobs `completed` and 2 `failed` with
`failure_class infrastructure` ("Failed to get PID from job launch", empty pid file, "no logs available") -- jobs 5
and 6, `V-_s1` and `S_s2`; both job scripts nevertheless ran to the end on the node: their consoles say `device
cuda`, their JSON rows carry all 800 frames and their ledger NPZs exist, and the analysis's per-run checks pass them.
The receipt is therefore 18 valid runs by content and 16 + 2 by the scheduler's own accounting.

### 3.1 The family (`compare.csv`)

| test | key | stim | null | predicted | n | verdict | verdict (Holm, sign) | diff | z (ref SD) | Welch t | p | p_holm | all separated |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1_GLNO_LR_V-_vs_S | GLNO_LR_hz | V- | S | negative | 6 v 6 | result | **result** | -2.0665 | -1557.39 | -7.83 | 0.0022 | 0.0065 | True |
| 2_GLNO_LR_V_vs_V- | GLNO_LR_hz | V | V- | positive | 6 v 6 | result | **result** | +4.2848 | +6.63 | +10.61 | 0.0022 | 0.0065 | True |
| 3_PS196b_LR_V_vs_V- | PS196b_LR_hz | V | V- | negative | 6 v 6 | result | **result** | -36.1679 | -54.66 | -47.58 | 0.0022 | 0.0065 | True |

All three carry the predicted sign; all six values of the stim arm lie beyond all six of the null arm in each test.
Primary 1's z (-1557) is the -2.0665 Hz difference divided by S's near-zero SD (0.0013 Hz), the same reading as round
7's +1671.8; the symmetric Welch t (-7.83) is the number to carry.

### 3.2 Per arm (`descriptive.csv`, mean +/- sample SD over seeds; per-side turn-window pedestals)

| arm | GLNO L-R | GLNO L / R | PEN L-R | PEN L / R | PS196b L-R | PS196b L / R | AFF L-R | AFF L / R | DNa02 L-R | frac confined post |
|---|---|---|---|---|---|---|---|---|---|---|
| S | -0.0000 +/- 0.0013 | 0.02 / 0.02 | +0.0168 +/- 0.0200 | 0.03 / 0.01 | +0.0000 +/- 0.0000 | 0.00 / 0.00 | NA | NA / NA | +0.0000 +/- 0.0000 | 0.0000 |
| V | +2.2183 +/- 0.7486 | 2.59 / 0.37 | +0.0201 +/- 0.0453 | 0.13 / 0.11 | -18.1618 +/- 1.7403 | 0.00 / 18.16 | +42.6139 +/- 2.4907 | 42.61 / 0.00 | +0.0000 +/- 0.0000 | 0.0000 |
| V- | -2.0665 +/- 0.6461 | 0.02 / 2.09 | +0.0318 +/- 0.0309 | 0.05 / 0.01 | +18.0061 +/- 0.6617 | 18.01 / 0.00 | -44.2544 +/- 1.8199 | 0.00 / 44.25 | +0.0000 +/- 0.0000 | 0.0000 |

The pedestal is the point: V's +2.2183 Hz is GLNO_L 2.59 over GLNO_R 0.37; V-'s -2.0665 Hz is GLNO_R 2.09 over GLNO_L
0.02, and V-'s GLNO_L values equal S's GLNO_L values exactly in all six seeds (0.0000, 0.0005, 0.1101, 0.0011, 0.0002,
0.0010; `per_seed.csv`) -- the side the flipped afferent does not drive is untouched, seed for seed. The afferent
(AFF) and PS196_b reports mirror to within their scatter (42.61 / 0.00 -> 0.00 / 44.25; 0.00 / 18.16 -> 18.01 / 0.00);
the GLNO response is 7 % smaller in the flipped direction (2.07 vs 2.22 Hz), a difference well inside the
across-seed SDs (0.65 / 0.75) and given no verdict. PEN L-R and DNa02 L-R are as in round 7: at the pedestal
(PEN 0.13 / 0.11 Hz in V) and zero. No arm has a confined bump (frac_confined_post 0 in all 18 runs), as in cx8r's S
and V: this protocol measures the side report, not a bump.

### 3.3 Per-seed lists (`per_seed.csv`, columns `arm,key,seeds,values`; pasted, INTERP 10.4 rule 28)

| arm | key | seeds | values |
|---|---|---|---|
| S | GLNO_LR_hz | 0,1,2,3,4,5 | -0.0018,-0.0014,0.0015,0.0003,0.0002,0.0010 |
| S | GLNO_L_hz_turn | 0,1,2,3,4,5 | 0.0000,0.0005,0.1101,0.0011,0.0002,0.0010 |
| S | GLNO_R_hz_turn | 0,1,2,3,4,5 | 0.0018,0.0020,0.1086,0.0008,0.0000,0.0000 |
| S | PEN_LR_hz | 0,1,2,3,4,5 | 0.0322,0.0002,0.0393,0.0333,0.0001,-0.0041 |
| S | PS196b_LR_hz | 0,1,2,3,4,5 | 0.0000,0.0000,0.0000,0.0000,0.0000,0.0000 |
| V | GLNO_LR_hz | 0,1,2,3,4,5 | 2.0053,2.6428,2.2497,0.8462,2.6170,2.9488 |
| V | GLNO_L_hz_turn | 0,1,2,3,4,5 | 2.3370,3.9572,2.3583,0.8470,3.1106,2.9488 |
| V | GLNO_R_hz_turn | 0,1,2,3,4,5 | 0.3316,1.3144,0.1086,0.0008,0.4936,0.0000 |
| V | PEN_LR_hz | 0,1,2,3,4,5 | 0.0306,0.0823,0.0379,0.0327,-0.0154,-0.0472 |
| V | PS196b_LR_hz | 0,1,2,3,4,5 | -18.6666,-18.0815,-18.3178,-14.8798,-19.9797,-19.0456 |
| V | AFF_LR_hz | 0,1,2,3,4,5 | 43.3540,43.4948,41.8607,38.0538,45.3305,43.5896 |
| V- | GLNO_LR_hz | 0,1,2,3,4,5 | -1.4627,-1.7747,-2.3811,-2.1203,-3.1658,-1.4946 |
| V- | GLNO_L_hz_turn | 0,1,2,3,4,5 | 0.0000,0.0005,0.1101,0.0011,0.0002,0.0010 |
| V- | GLNO_R_hz_turn | 0,1,2,3,4,5 | 1.4627,1.7752,2.4912,2.1214,3.1661,1.4956 |
| V- | PEN_LR_hz | 0,1,2,3,4,5 | 0.0466,0.0002,0.0852,0.0325,0.0153,0.0110 |
| V- | PS196b_LR_hz | 0,1,2,3,4,5 | 18.1393,16.9619,17.8107,18.3575,18.9468,17.8204 |
| V- | AFF_LR_hz | 0,1,2,3,4,5 | -44.6523,-41.9164,-42.5673,-44.9742,-47.0013,-44.4152 |

DNa02 L-R, DNa02 L and R and frac_confined_post are 0.0000 in all 18 runs (`per_seed.csv`).

### 3.4 Replication of cx8r (`replication.csv`)

cx8r's S and V ran on node 1 on 2026-09-15; cx9's ran on node 2 on 2026-09-17 from a different submission. **76 of
76 (arm, key) rows are exactly equal in all six seeds** (every one of the 38 keys of both arms; max |diff| 0). With
[determinism_gate.md](determinism_gate.md)'s same-GPU repeat this makes the cx_wedge protocol reproduced exactly in
three draws across two nodes and three submissions. It is recorded as an observation about the software; no verdict
above rests on it, and it says nothing about any room (which repeats nowhere).

## 4. Answer

The GLNO side report follows the afferent's sign where round 7 measured it: with the afferent alone on the shipped
ring, sign -1 gives GLNO L-R -2.0665 +/- 0.6461 Hz against S's 0.0000 +/- 0.0013 (result, Holm p 0.0065), V minus V- is
+4.2848 Hz (result), and the PS196_b report reverses with it (-36.1679 Hz, result); all six seeds separate in each
test. The response is one-sided at the pedestal: V- drives GLNO_R (2.09 Hz) and leaves GLNO_L exactly at S's values
in every seed, the mirror of V (GLNO_L 2.59 / GLNO_R 0.37). This closes the sign-specificity gap of round 7's primary 3
for the unheld ring only. It does not touch the held arms: HGV-'s non-reversal (+0.69 +/- 1.91 Hz) stands as
measured, so what the hold plus relabel does to the crossing is still open; nothing here is a bump, a following
compass, a receptor sign or an adoption. cx9's S and V reproduce cx8r's values exactly in 76/76 rows across two nodes.

## 5. Reproduction

```sh
PYTHONIOENCODING=utf-8 python scripts/cx_sign_control.py --analyse --runs out/cx9 --out out/cx9/analysis --reference out/cx8r
```

Committed: `out/cx9/{batch.sh,arms.json,predeclared.json}` and `out/cx9/analysis/{runs,per_seed,compare,descriptive,replication}.csv`,
`analysis.md`, `analysis.json`. The run JSONs, NPZs and consoles stay ignored (host paths). cx8r's runs are read from
`out/cx8r/` (ignored; the owner's copy).

## 6. Author self-review (the independent skeptic pass is not this section)

- The three primaries are the ones frozen; the sign rule (`result, opposite sign`) was frozen with them and did not
  bite. Holm m = 3 over p = 0.0022 x 3 gives 0.0065 for all three: the exact-U floor, as expected for fully separated
  6 v 6 samples; nothing finer than "separated" is claimed.
- `compare`'s z is orientation-dependent (the round-7 skeptic's point); the Welch t is printed beside it and the
  verdicts also hold in the symmetric reading (|t| 7.8-47.6).
- The 7 % asymmetry between |V| and |V-| at GLNO is inside the SDs and is not read; a reader who wants it tested needs
  a declared contrast, not this one.
- "V-'s GLNO_L equals S's GLNO_L exactly" is a statement about identical draws on a repeating protocol, not a
  biological invariance: the unstimulated side receives the same Poisson stream in both arms.
- Two jobs are `failed / infrastructure` at the scheduler while their outputs are complete; a strict reader may prefer
  those two seeds re-run. The per-run checks (frames, metrics, NPZ, console, hashes) are what the analysis trusts, and
  the family's verdicts do not change if seeds 1 (V-) and 2 (S) are dropped: every remaining value still separates.
- The replication check is exact equality of numbers produced on two nodes of the same GPU model; it is not a
  cross-hardware claim.
- Nothing adopted; raw untouched; no default changed; no compass room follows from this.

## Report

```yaml
summary: |-
  The GLNO side report follows the afferent's sign where round 7 measured it: with the afferent alone on the shipped
  ring, sign -1 gives GLNO L-R -2.0665 +/- 0.6461 Hz against S's 0.0000 +/- 0.0013 (result, Holm p 0.0065), V minus V- is
  +4.2848 Hz (result), and the PS196_b report reverses with it (-36.1679 Hz, result); all six seeds separate in each
  test. The response is one-sided at the pedestal: V- drives GLNO_R (2.09 Hz) and leaves GLNO_L exactly at S's values
  in every seed, the mirror of V (GLNO_L 2.59 / GLNO_R 0.37). This closes the sign-specificity gap of round 7's primary 3
  for the unheld ring only. It does not touch the held arms: HGV-'s non-reversal (+0.69 +/- 1.91 Hz) stands as
  measured, so what the hold plus relabel does to the crossing is still open; nothing here is a bump, a following
  compass, a receptor sign or an adoption. cx9's S and V reproduce cx8r's values exactly in 76/76 rows across two nodes.
key_claims:
- V- minus S GLNO L-R -2.0665 Hz, result (Holm p 0.0065, Welch t -7.83, all six separated), the predicted sign.
- V minus V- GLNO L-R +4.2848 Hz and PS196b L-R -36.1679 Hz, both results with the predicted sign.
- The flipped afferent drives the other side and leaves the unstimulated side at S's exact values (pedestals quoted).
- cx9's S and V equal cx8r's in 76/76 (arm, key) rows across two nodes; recorded as an observation.
validation:
- 18 runs, 18/18 consoles device cuda, NVIDIA B200 x 18, cache ef23cc27 x 18, resolved LIF equal to the frozen record, every loaded source at the predeclared hash, 0 analysis problems.
- Frozen before submission (out/cx9/predeclared.json 2026-09-17T23:43:39Z; run cx9-2daa4d).
- Scheduler receipt 16 completed + 2 failed/infrastructure (launch PID tracking); both of those runs are complete by content and the verdicts hold without them.
recommendations:
- Nothing adopted; the held-arm crossing (HGV-) is the remaining sign question and needs its own declared arm pair.
open_questions:
- Why the hold plus relabel removes the sign specificity that the unheld ring shows.
```
