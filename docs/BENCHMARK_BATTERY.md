# Behavioural benchmark battery

A behaviour-level target for the model (and for approximations of it), from a colleague's proposal,
ordered by cost to try. Each assay names the discriminating variable and how the plain model scores
today. `pass` = the connectome model produces it with the plain body; `partial` = the neural signal is
there but the motor path is not, or one branch of the assay works; `program` = only a behaviour
program / module produces it; `gap` = a documented model deficit; `untested` = cheap to run, not run.
Numbers refer to `docs/NOTES.md`. `scripts/benchmark.py` is the harness; assays become sections there.
The machine-readable form of this table is the expectation ledger -- `flyverse/data/expected_responses.csv`
(119 rows: population, stimulus, quantity, the literature citation, the file that measured it here, and the
criterion), scored by `scripts/interp_ledger.py` into PASS / FAIL / MISSING per row and arm
(`docs/audits/interp_ledger.md`; that audit's "110 rows" predates the nine `lit.*` rows). A row there is an
expectation with a citation, never a fitting target. The nine `lit.*` rows added from Neurome's 2026-09-13 intake
(LC11 / LC10a size tuning, LC11 receptors and the Rdl perturbation, T2 / T3 ON+OFF, T3 -> LC11 excitation, the
Tanaka & Clark pooling hypothesis) all carry op `report`: **literature constraints from calcium imaging and
perturbation experiments, recorded and never scored, not MaleCNS firing-rate targets**.

| # | assay | discriminating variable | status | evidence / what is missing |
|---|---|---|---|---|
| 1 | optomotor response (rotating drum) | sign of the turn vs drum direction | partial | `screen_rotation.py`: HSN d' 4.2, DNp20 3.1, HSE 2.2 flip with sustained yaw. DNa02 is silent (0.01 Hz), so no path from the horizontal system to the legs in the model; the readout can steer, the connectome cannot yet. The HS -> DNa02 half is now localized: DNa02 is not merely unlit but held below threshold by sign-correct tonic inhibition (-326 / -392 mV/s = -1.6 / -2.0 mV of steady conductance against a 7 mV gap), and the LLPC1 / HSS motion asymmetry that does arrive is cancelled by GABAergic LPT22 at corr -0.86 (`docs/audits/deficit_turning.md`). |
| 2a | looming escape, short mode (giant fibre) | fast expansion -> GF burst -> takeoff, no wing raise | pass | GF 34-56 Hz on 5 of 6 looms, threshold 33 (re-derived after LPi x4); self-motion no longer fakes it (walking 99th percentile 22.5 Hz). `probe_loom.py`. |
| 2b | looming escape, long mode | slow expansion -> GF-independent, wing raise, directed | partial | Expansion-rate sweep (3 cm ball, l/v 15-250 ms, 3 seeds, pre-loom baseline): the wing power MNs rise more for slower looms (+41 -> +53 Hz) and earlier (0.5 s after arrival at l/v 15 ms; 0.9 s and 2.0 s *before* arrival at 120 / 250 ms) -- the wing-raise signature scales the right way -- but the GF still peaks +27-41 Hz 50-140 ms after arrival at every rate and the body takes the short-mode escape in 13 of 15 runs: no mode switch. The model's GF responds to final size, not expansion rate. |
| 3 | landing response | gentle expansion -> leg extension, no takeoff | fail | Stop-short approaches (the ball halts at 8-15 cm, final size 23-41 deg, l/v 60-250 ms, 2 seeds): GF +7.5 to +15 Hz -- under threshold, no takeoff in any run (the short mode is size-gated in the right direction); wing power +35 to +58 Hz without a jump (a wing-raise-like preparation); leg MNs +2.7 to +5.1 Hz -- no leg extension, so no landing. |
| 4 | cast-and-surge in an intermittent plume | surge on contact, cast on loss, alternation | program (compass structure now found) | plume puffs at 1.5 s; the LH apple / berry channels follow the puffs (model level); surge / cast is `AnemotaxisProgram` / `cx.CompassSteering`. The connectome's steering integration (CX) is silent at the defaults, but the session-9 audit found a working ring attractor under Delta7 -> EPG x15 / recurrence x2 / no compass adaptation (`docs/audits/cx_wedge.md`); running it with senses and through PFL3 is the next step. The steering the program stands in for is now localized on both halves: PFL3 -> DNa02 works when driven (+45 Hz at 150 Hz in the atlas) but PFL3 is 0.000 Hz in the room (`docs/audits/deficit_turning.md`), and the ring has no rotation input to steer with, because PS196_b never fires and GLNO -> PEN is sign 0 (`docs/audits/deficit_rotation.md`). Dynamics round 2 supplied the missing input and the answer did not change: with the transducer on, PS196_b fires (0.19/0.52 -> 1.44/1.51 Hz) and GLNO with it (0.03/0.04 -> 0.66/0.66), yet the bump drifts -0.005..+0.004 wedges/s against 4.0 ideal in every arm, and under the labelled Coriolis stop-gap -- PS196_b driven to 11 Hz, 13x rest -- **its L-R moves the same way in both turn directions**. The block is no longer a missing input but an **unsigned route**: the only afferent class two steps from PS196_b is the haltere SApp, and `MotorRates.haltere` is one bilateral number. `docs/audits/vnc_drive.md` 6. |
| 5 | grooming cascade | anterior -> posterior priority under whole-body dust | untested (new thread) | bristle sensory neurons by segment are in the connectome (class mechanosensory); leg-MN readouts exist; the assay is a stimulation screen (stimulate bristle sets, read leg-MN patterns and their suppression). |
| 6 | local search after food loss | re-looping around the last food site | program (compass structure now found) | `AnemotaxisProgram`'s search; the plain model has no path-integration-like behaviour (CX silent). Why the CX is silent is now a named pair of facts: the bump receives no rotation signal (PS196_b silent, GLNO -> PEN sign 0, the eye's report arriving direction-blind -- `docs/audits/deficit_rotation.md`) and its output stage PFL3 sits at 0.000 Hz although PFL3 -> DNa02 works when driven (`docs/audits/deficit_turning.md`). Dynamics round 2 supplied the missing input and the answer did not change: with the transducer on, PS196_b fires (0.19/0.52 -> 1.44/1.51 Hz) and GLNO with it (0.03/0.04 -> 0.66/0.66), yet the bump drifts -0.005..+0.004 wedges/s against 4.0 ideal in every arm, and under the labelled Coriolis stop-gap -- PS196_b driven to 11 Hz, 13x rest -- **its L-R moves the same way in both turn directions**. The block is no longer a missing input but an **unsigned route**: the only afferent class two steps from PS196_b is the haltere SApp, and `MotorRates.haltere` is one bilateral number. `docs/audits/vnc_drive.md` 6. |
| 7 | hunger-modulated thresholds | starvation sharpens sugar sensitivity, raises plume vigour, crosses a bitter barrier | gap | metabolism exists (`body.Metabolism`), sugar / bitter PER replicates (Shiu rules 124 -> 2 Hz; calibrated 4.6 -> 0). But dopamine / octopamine / serotonin synapses are sign 0 in the model: neuromodulation has no route. `docs/audits/nt_audit.md` quantifies it. |
| + | spontaneous turning (free walking) | yaw-rate SD / path straightness of the plain fly in the room vs free-walking Drosophila (tens of deg/s; frequent saccadic turns) | gap | `scripts/probe_walk_straightness.py`: median yaw SD 1.6-2.8 deg/s, straightness 0.99-1.00 in every weight configuration since session 9 (before that, turns came from an optomotor readout responding to self-motion -- an artefact, removed). No connectome-level source of turning at rest; the compass -> PFL3 -> DNa02 route is the candidate (dynamics round 1). The connectome-level source is now named: DNa02's operating point (net -326 / -392 mV/s of sign-correct tonic inhibition from GABA / glutamate cells at 3-14 Hz) plus three silent lateralised routes (PFL3 and AOTU015 / AOTU001 at 0.000 Hz; most of LLPC1 never firing and LPT22 cancelling what survives) and a fourth, the wind, live and lateralised (r -0.61) but ~90x under dose. The candidate mechanism is proprioceptive / haltere drive of the VNC sensory neurons, every one of which is at 0 Hz in the room -- a body-model transducer, not a gain (`docs/audits/deficit_turning.md`). Dynamics round 2 built that transducer and ran it (`docs/audits/proprioception_transducer.md`, `vnc_drive.md`; opt-in, default OFF): the afferents now fire and the route works -- AN04B003 0.589/0.314 -> 3.713/3.690 Hz, the sided excitatory term on DNa02 +9.09 -> +57.35 mV/s, the tonic VNC inhibitors relaxing by +5.0 to +7.4 -- but it is cancelled on the spot by the haltere afferents' own second-step inhibition (PS059 -90.8 -> -113.5) and **DNa02 does not fire**: net -303 -> -235 mV/s = -1.52 -> -1.17 mV of steady `g` against a 7.0 mV gap, 2-3 % of the required dose. Yaw-rate SD does rise 2.85 -> 3.32 deg/s and straightness falls 0.9956 -> 0.9818, but **entirely through the leg-MN branch of `body.Locomotion`'s hand-written yaw law** (leg L-R +0.151 -> +0.301 Hz, a fixed positive offset that is positive in all 400 fly-runs and never changes sign, even at 1.6 rad/s of imposed counter-turn) -- a bias, not turning, and not a status change. The gap stands; the next mechanism is named: a leg cycle in `body.py` (which would make the leg channels sided) and a side-split haltere MN readout in `motor.py`. |
| + | small-object detection | does a 5-30 deg ball / apple reach the small-object channel (LC11 / LC10a), and at what angular size | gap | The figure is carried by the lamina, medulla, T4c/d and LPLC2 and lost at LC11 / LC10a's inputs. Localized as three facts: ON/OFF excitatory convergence at T3 (holding either carrier class at 0 restores T3, z +3.5 / +4.8), the stochastic optic feedback destroying the residual, then l1 pooling over 94 / 32 columns at the LC cells. **Round 2 replaced the scene ladder with the matched assay Neurome asked for** (`docs/audits/object_matched_assay.md`; centre elevation, distance, angular diameter and angular speed held per frame -- realised deviation 0.0 deg, 1.4e-14 deg, 40.000 deg/s -- against the old ladder's 0.88 -> 13.71 deg elevation and 45.8 -> 18.8 deg/s; radiance captured in-loop for both arms). **On that assay, at 6 runs per arm in one submission with predeclared 12-member families, no size preference is called for either type**: LC11 12/12 `null` (smallest p 0.180), LC10a 11 `null` + one `result` at 30 deg (z +3.37, p 0.0087) that **fails Holm at p_holm 0.104** and sits inside LC10a's own 15-30 deg target (Schretter 2024), not LC11's 8.8 deg vertical / ~4.4 deg width (Keles & Frye 2017). `spikes_median` is exactly 0 in every arm: the spike half of each family is uninformative, and the populations are not silent. Upstream, `diff_signed_best_cell` reaches `result` surviving Holm at T2 / Tm5Y from 11 deg and T3 / TmY21 from 20 deg and rises monotonically with size (T2 z +3.8 -> +23.0), while the population `diff_signed_mean` stays `null` at every rung -- the cancellation signature, now without the geometry confound. **The fixed-anatomy model comparison found no mechanism that passes** (`docs/audits/object_compare_r2.md`, 8 arms x 5 runs + a 7-stimulus specificity battery + 3 bench draws, one submission): per-stream rectification is the only arm that carries the T3 / T2 carrier figure (6-10x base at the small rungs) and it **releases bar, grating and flicker responses at LC11** (grating 0.611 -> 3.202 mV, flicker 4.71 -> 10.67 Hz), creates a figure that grows with size, moves only the max over cells (the per-body windowed T3 / T2 medians are `null` against base in all 48 rows) and shifts the operating point; adaptation at 100 / 300 ms is inert; spatial suppression produces no figure and costs `loom_escape.GF_peak_hz` (50.0 -> 31.2 Hz) and `loom_escape.escapes` (1.0 -> 0.33). **LC11 output follows in no arm. Nothing is adopted; every hook is opt-in and bit-identical off.** A finding of the battery in its own right: the shipped LC11 already responds to a 30-deg grating (0.611 mV vs a blank/blank null of 0.085, z +21.5), a 7-deg bar and 2-Hz full-field flicker while an 11-deg dark square sits at the null -- the opposite selectivity from the animal. Caveats on the round: effective retinal contrast is not matched across the sphere ladder (extreme relative change -0.50 -> -0.95), the RF localizer fits 0 of 143 LC11 bodies at `z_min` 5 so 405 of 418 LC windows are anatomical, and arm was confounded with cluster box (the rectification magnitude is confirmed on base's own GPU model by the two combination arms; a same-device `base` re-run is owed). Exported to Neurome under `flyverse.neurome.export/2`: `out/export/objr2-{ship,fb0}-d*` + `objr2-ladder-20260914T024906Z-035363c0` (the 2026-09-14 re-emit; the 2026-09-13 set is superseded). `docs/NEUROME_INTERFACE.md` 3c. |
| + | spontaneous take-off (free walking, no loom) | voluntary take-offs per 1,000 fly-s (wing power >= 50 Hz held 0.3 s), split from GF escape at 33 Hz | gap (no animal reference) | `benchmark.py --sections hops` (opt-in, 2,400 fly-s) and the batched room protocol. The shipped default makes 3.0 voluntary + 2.0 escape per 1,000 fly-s against off's 0 + 0.5 (18 batches, 86,400 fly-s: **1** voluntary take-off under off). Attributed in dynamics round 2 to one class of the receptor signs: the 17,256 two-source **histamine silencings** (90 % photoreceptor -> medulla) reproduce the default on hops, voluntary and the GF median and beat off on all three (7.21x hops pooled over 7 runs/arm); the larger 27,207-entry glutamate class, and an entry-matched random draw from the carrying class, both reproduce off. **The escape channel is unattributable** (at 7 v 7 even off vs shipped is `compare`-null on it) and **dose in postsynaptic cells touched is not excluded and cannot be by this design**. There is no animal reference for the rate in this arena, so this is a gap, not a FAIL. `docs/audits/receptor_integration.md` G.5-G.6. |
| + | flight saccades | straight segments, ~90 deg saccades on expansion | untested | flight model has steering MNs -> yaw and roll; no measurement yet. |

## Order of work implied

2b and 3 first (same stimulus family as 2a, readouts exist, model-level); then 7's prerequisite (a
sign for the monoamines, or an explicit neuromodulation model) since it also blocks any
state-dependence; 5 as the next emergent test; 1, 4 and 6 wait on the central-complex thread.

A `gap` or `partial` here is now a **ledger row**, not a prose note: the expectation lives in
`flyverse/data/expected_responses.csv` with its citation and its criterion, and `scripts/interp_ledger.py` scores it
from whatever file last measured it. The first step on a failing row is `docs/INTERP.md` section 10 -- the
seven-step procedure (ledger row -> paths -> one recorded batch -> trace then decompose at the lost stage ->
counterfactual arms -> the lesion matrix -> write-up and export), which localizes the deficit to a named
population, a named link and a kind of fact, with a data-driven next measurement --
and never a tuned bound: a row whose criterion is unfit carries op `report` and is recorded without being
scored. The standing example is the 50 Hz `walk.power_max` bound (`walk.power_MN.rate_max_hz` in the
ledger), and dynamics round 2 decided it from the data (`docs/audits/anti_runaway.md` round 6): 48.48 Hz in
12 of 12 shipped draws with a 1.52 Hz margin that is **inside the worst single-arm scatter on record**,
non-monotone in the one three-point gain scan, moved in opposite directions by the two path gains, and
**rank-anticorrelated with the room take-off rate (Spearman -0.600) across the four hold arms** -- the two
arms that FAIL it are the two quietest rooms. `scripts/benchmark.py:85` now carries the `notnone` form
`loom.escape_cm` already uses, so the number stays in the table and the JSON and has stopped being a
pass/fail; the scored take-off criterion is `walk.power_sustained_hz` (unchanged), which carries the real
referent (`body.Flight.takeoff_power_hz`, 50 Hz held 0.3 s). Note the `notnone` row still counts as a PASS
in the tally (`benchmark.py:135`) -- de-scored, not removed. The three rows localized
this way so far are optomotor / spontaneous turning (`deficit_turning.md`), small-object detection
(`deficit_object.md`) and the compass' rotation input (`deficit_rotation.md`); none of them changed a default.

Session-9 audit notes: `scripts/benchmark.py` now scores assays 1, 2a, 3 (via 2b's readouts), 7's taste half and the
odour gate in one run (`docs/audits/benchmark_suite.md`); the free-walking loom sections predate the `update_loom`
direction fix (the ball now approaches along the fly's left at any heading) and are to be re-measured over six seeds.
